package com.omar.sani.empleatec.ui.home.verUser;

import com.omar.sani.empleatec.controlador.database.login.dbUsuario.Usuario;

import java.util.List;

public class ValidarUsuarios {

    tarjetaPersentacion tarjetaPersentacion = new  tarjetaPersentacion();
    // Método para procesar la lista de usuarios
    public void procesarUsuarios(List<Usuario> usuarios) {
        for (Usuario usuario : usuarios) {
            //en esta parte crea un nuevo objeto de la clase mostrar
        }
    }
}
